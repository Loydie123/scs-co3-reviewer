import type { ReviewerContent } from "@/types";
import { DOMAIN_IDS } from "@/types";

export const dataProtectionContent: ReviewerContent = {
  domain: DOMAIN_IDS.DATA_PROTECTION,
  sections: [
    {
      id: "overview",
      title: "Data Protection Overview",
      content: [
        "Data Protection focuses on encryption, key management, secrets handling, and data classification. This domain covers protecting data at rest (stored data) and in transit (data moving between systems) across AWS services.",
        "The core principle is defense in depth: encrypt data at multiple layers, use strong key management, rotate credentials regularly, and classify data based on sensitivity to apply appropriate controls.",
        "AWS provides multiple encryption options for different use cases, from fully managed (AWS-owned keys) to customer-controlled (customer-managed keys in KMS or CloudHSM).",
      ],
      keyPoints: [
        "Always encrypt sensitive data at rest and in transit",
        "Use AWS KMS for centralized key management and audit trails",
        "Rotate keys and secrets regularly to limit exposure",
        "Classify data based on sensitivity (public, internal, confidential, restricted)",
        "Understand encryption options: SSE-S3, SSE-KMS, SSE-C, client-side encryption",
      ],
    },
    {
      id: "kms",
      title: "AWS Key Management Service (KMS)",
      content: [
        "Concept: KMS is a managed service for creating and controlling encryption keys used to encrypt data across AWS services. It provides centralized key management, automatic key rotation, and detailed audit trails via CloudTrail. KMS uses Hardware Security Modules (HSMs) that are FIPS 140-2 validated.",
        "When to use: Use KMS when you need centralized key management with audit trails, integration with AWS services (S3, EBS, RDS, etc.), automatic key rotation, and fine-grained access control via key policies. Essential for compliance requirements that mandate key management controls.",
        "When NOT to use: Don't use KMS if you need single-tenant HSMs (use CloudHSM), if you need to export keys (KMS keys never leave AWS), or if you need keys for non-AWS systems without API access. KMS has API rate limits that may not suit extremely high-throughput scenarios.",
        "Real-world scenario: A healthcare company needs to encrypt patient records in S3 and RDS with centralized key management and audit trails for HIPAA compliance. They create customer-managed KMS keys with key policies restricting access to specific roles, enable automatic rotation, and use CloudTrail to audit all key usage.",
        "Key Types: AWS-owned keys (fully managed by AWS, no visibility), AWS-managed keys (created by AWS services, visible but not manageable), Customer-managed keys (full control, rotation, policies). Symmetric keys (same key for encrypt/decrypt) vs Asymmetric keys (public/private key pairs for encryption or signing).",
      ],
      services: ["KMS", "CloudHSM", "CloudTrail", "IAM"],
      keyPoints: [
        "Customer-managed keys (CMK) give you full control over rotation, policies, and deletion",
        "AWS-managed keys are created by AWS services (like aws/s3), free but less control",
        "AWS-owned keys are used by AWS internally, not visible in your account",
        "Key policies are resource-based policies that control access to KMS keys",
        "Automatic key rotation (yearly) for symmetric customer-managed keys",
        "CloudTrail logs all KMS API calls for comprehensive auditing",
        "Keys are regional—must replicate for multi-region use",
      ],
      examTips: [
        "Know the three key types: AWS-owned, AWS-managed, Customer-managed",
        "Key policies are required—IAM policies alone aren't sufficient",
        "Automatic rotation rotates key material, not the key ID",
        "KMS keys never leave AWS unencrypted",
        "Envelope encryption: KMS encrypts data keys, not data directly",
      ],
    },
    {
      id: "kms-vs-cloudhsm",
      title: "🔍 Comparison: KMS vs CloudHSM",
      content: [
        "AWS KMS vs AWS CloudHSM:",
        "• KMS: Multi-tenant HSM service managed by AWS. Shared infrastructure with logical isolation. Easier to use, integrates with AWS services, automatic key rotation, pay per API call.",
        "• CloudHSM: Single-tenant HSM dedicated to your VPC. You manage the HSM, AWS manages hardware. Full control over keys, can export keys, supports more cryptographic algorithms, higher performance.",
        "",
        "When to use KMS:",
        "• Need integration with AWS services (S3, EBS, RDS, etc.)",
        "• Want AWS to manage HSM operations and patching",
        "• Need automatic key rotation",
        "• Require CloudTrail audit logs",
        "• Cost-effective for most use cases",
        "",
        "When to use CloudHSM:",
        "• Need single-tenant HSM for compliance (FIPS 140-2 Level 3)",
        "• Must export keys or use keys outside AWS",
        "• Require specific cryptographic algorithms not supported by KMS",
        "• Need very high transaction rates (>10,000 TPS)",
        "• Must have full control over HSM and key lifecycle",
        "",
        "Key difference: KMS is for AWS service integration and ease of use. CloudHSM is for regulatory requirements demanding single-tenant HSMs or key portability.",
      ],
      keyPoints: [
        "KMS = Multi-tenant, AWS-managed, integrated with AWS services",
        "CloudHSM = Single-tenant, customer-managed, dedicated HSM in your VPC",
        "KMS keys cannot be exported; CloudHSM keys can be",
        "KMS is easier and cheaper; CloudHSM offers more control",
        "Both are FIPS 140-2 validated (KMS Level 2, CloudHSM Level 3)",
      ],
      examTips: [
        "If question mentions 'single-tenant HSM' or 'export keys' → CloudHSM",
        "If question mentions AWS service integration or ease of use → KMS",
        "CloudHSM requires you to manage HSM cluster and keys",
        "KMS is the default choice unless specific CloudHSM requirements exist",
      ],
    },
    {
      id: "s3-encryption",
      title: "S3 Encryption Options",
      content: [
        "Concept: S3 offers multiple server-side encryption (SSE) options and client-side encryption. Server-side encryption means S3 encrypts data after receiving it and decrypts before sending it. Client-side encryption means you encrypt data before sending to S3.",
        "SSE-S3 (AES-256): S3 manages encryption keys. Simplest option, no additional cost, keys managed entirely by AWS. Each object encrypted with unique key, keys encrypted with master key that's rotated regularly.",
        "SSE-KMS: S3 uses KMS keys to encrypt objects. Provides audit trail via CloudTrail, key policies for access control, automatic key rotation. Additional KMS API costs. Can use AWS-managed key (aws/s3) or customer-managed key.",
        "SSE-C (Customer-provided keys): You provide encryption key with each request. S3 encrypts/decrypts but doesn't store the key. You manage keys outside AWS. Must use HTTPS. Key must be provided with every GET/PUT request.",
        "Client-side encryption: Encrypt data before uploading to S3. You manage entire encryption process and keys. S3 stores encrypted data without knowing about encryption. Maximum control but more complexity.",
      ],
      services: ["S3", "KMS", "CloudTrail"],
      keyPoints: [
        "SSE-S3: AWS manages everything, simplest, no extra cost, AES-256",
        "SSE-KMS: Audit trail, key policies, rotation, additional cost for KMS API calls",
        "SSE-C: You provide keys per request, S3 doesn't store keys, HTTPS required",
        "Client-side: Encrypt before upload, maximum control, you manage keys",
        "Default encryption can be set at bucket level",
        "Bucket policies can enforce encryption (deny if not encrypted)",
      ],
      examTips: [
        "SSE-S3 is the default and simplest option",
        "SSE-KMS when you need audit trails or key policies",
        "SSE-C when you must control keys outside AWS",
        "Client-side when you need end-to-end encryption",
        "Know how to enforce encryption with bucket policies",
      ],
    },
    {
      id: "comparison-s3-encryption",
      title: "🔍 Comparison: SSE-S3 vs SSE-KMS vs SSE-C",
      content: [
        "SSE-S3 vs SSE-KMS vs SSE-C:",
        "",
        "SSE-S3 (Server-Side Encryption with S3-Managed Keys):",
        "• Pros: Simplest to use, no additional cost, fully managed by AWS, AES-256 encryption",
        "• Cons: No audit trail for key usage, no control over keys, can't set key policies",
        "• Use when: You need simple encryption without key management overhead",
        "",
        "SSE-KMS (Server-Side Encryption with KMS Keys):",
        "• Pros: CloudTrail audit logs, key policies for access control, automatic rotation, can use customer-managed keys",
        "• Cons: Additional KMS API costs, API rate limits may affect high-throughput workloads",
        "• Use when: You need audit trails, key policies, or compliance requires KMS",
        "",
        "SSE-C (Server-Side Encryption with Customer-Provided Keys):",
        "• Pros: You control keys outside AWS, S3 doesn't store keys, keys can be rotated on your schedule",
        "• Cons: You must provide key with every request, HTTPS required, more complex to implement, no AWS key management",
        "• Use when: Compliance requires you to manage keys outside AWS",
        "",
        "Decision Matrix:",
        "• Need audit trail? → SSE-KMS",
        "• Need simplest option? → SSE-S3",
        "• Must control keys outside AWS? → SSE-C or Client-side",
        "• Need key policies? → SSE-KMS",
        "• Cost-sensitive high-throughput? → SSE-S3 (avoid KMS API costs)",
      ],
      keyPoints: [
        "SSE-S3 = Simplest, no extra cost, AWS manages keys",
        "SSE-KMS = Audit trail, key policies, customer control",
        "SSE-C = Customer provides keys, S3 doesn't store them",
        "All use AES-256 encryption",
        "Can enforce encryption type with bucket policies",
      ],
      examTips: [
        "If question asks for 'audit trail of key usage' → SSE-KMS",
        "If question asks for 'simplest encryption' → SSE-S3",
        "If question asks 'customer must control keys outside AWS' → SSE-C",
        "If question asks 'no additional cost' → SSE-S3",
        "Remember: SSE-C requires HTTPS and key with every request",
      ],
    },
    {
      id: "envelope-encryption",
      title: "Envelope Encryption",
      content: [
        "Concept: Envelope encryption is a practice where you encrypt data with a data key, then encrypt the data key with a master key. This provides performance benefits and security—only the small data key needs to be sent to KMS for decryption, not the entire dataset.",
        "How it works: 1) Call KMS GenerateDataKey to get a plaintext data key and encrypted data key. 2) Use plaintext data key to encrypt your data. 3) Store encrypted data key alongside encrypted data. 4) Delete plaintext data key from memory. 5) To decrypt: send encrypted data key to KMS, get plaintext data key, use it to decrypt data.",
        "Why use it: Encrypting large datasets directly with KMS would be slow and expensive (network latency, API costs). Envelope encryption allows you to encrypt locally with a data key, only using KMS to protect the data key itself. This is how S3, EBS, and other services implement encryption.",
        "Real-world scenario: An application encrypts 10GB of data. Instead of sending 10GB to KMS (impossible due to 4KB limit), it generates a data key from KMS, encrypts the 10GB locally with the data key, stores the encrypted data key with the data, and deletes the plaintext data key. To decrypt, it sends only the encrypted data key to KMS.",
      ],
      services: ["KMS", "S3", "EBS", "RDS"],
      keyPoints: [
        "Data key encrypts data, master key (KMS) encrypts data key",
        "KMS GenerateDataKey returns both plaintext and encrypted data key",
        "Store encrypted data key with encrypted data",
        "Never store plaintext data key—delete after use",
        "Decrypt: send encrypted data key to KMS, get plaintext data key",
        "Performance: only small data key goes to KMS, not entire dataset",
      ],
      examTips: [
        "Envelope encryption is how AWS services implement encryption at scale",
        "KMS has 4KB limit for direct encryption—envelope encryption bypasses this",
        "Data key is unique per object/volume",
        "Master key in KMS never leaves AWS unencrypted",
        "This is the pattern behind SSE-KMS in S3",
      ],
    },
    {
      id: "secrets-manager",
      title: "AWS Secrets Manager",
      content: [
        "Concept: Secrets Manager is a service for storing, rotating, and retrieving secrets like database credentials, API keys, and passwords. It provides automatic rotation for supported databases (RDS, DocumentDB, Redshift) and integrates with Lambda for custom rotation logic.",
        "When to use: Use Secrets Manager when you need automatic secret rotation, fine-grained access control via IAM, audit trails via CloudTrail, or cross-region replication of secrets. Essential for database credentials that must rotate regularly.",
        "When NOT to use: Don't use Secrets Manager for configuration data (use Parameter Store), for encryption keys (use KMS), or for secrets that don't need rotation (Parameter Store is cheaper). Secrets Manager has per-secret monthly costs.",
        "Real-world scenario: An application connects to RDS with database credentials. Instead of hardcoding credentials, store them in Secrets Manager with automatic 30-day rotation. Configure RDS to use Secrets Manager for authentication. When rotation occurs, Secrets Manager creates new credentials, updates RDS, and retires old credentials—all automatically.",
        "Comparison: Secrets Manager vs Parameter Store - Secrets Manager has automatic rotation, cross-region replication, and is designed for secrets. Parameter Store is cheaper, supports hierarchical storage, and is designed for configuration. Use Secrets Manager for rotating secrets, Parameter Store for static config.",
      ],
      services: ["Secrets Manager", "RDS", "Lambda", "KMS", "IAM"],
      keyPoints: [
        "Automatic rotation for RDS, DocumentDB, Redshift credentials",
        "Custom rotation via Lambda for other secrets",
        "Secrets encrypted with KMS at rest",
        "Fine-grained access control via IAM policies",
        "CloudTrail logs all secret access for auditing",
        "Cross-region replication for disaster recovery",
        "Versioning tracks secret changes over time",
      ],
      examTips: [
        "Secrets Manager for rotating secrets (especially database credentials)",
        "Parameter Store for static configuration and cheaper secrets",
        "Automatic rotation is the key differentiator",
        "Secrets Manager integrates with RDS for seamless rotation",
        "Know how to retrieve secrets in Lambda/EC2 applications",
      ],
    },
    {
      id: "parameter-store",
      title: "AWS Systems Manager Parameter Store",
      content: [
        "Concept: Parameter Store provides hierarchical storage for configuration data and secrets. It supports String, StringList, and SecureString (encrypted with KMS) parameter types. Free for standard parameters (up to 10,000), advanced parameters have additional features and costs.",
        "When to use: Use Parameter Store for application configuration, environment variables, license keys, and secrets that don't require automatic rotation. It's cost-effective for storing many parameters and supports hierarchical organization (e.g., /app/prod/db/password).",
        "When NOT to use: Don't use Parameter Store for secrets requiring automatic rotation (use Secrets Manager), for large binary data (use S3), or for encryption keys (use KMS). Standard parameters have 4KB size limit, advanced parameters support 8KB.",
        "Real-world scenario: A microservices application needs environment-specific configuration (dev, staging, prod). Store parameters hierarchically: /myapp/dev/api-url, /myapp/prod/api-url. Applications retrieve parameters based on environment. Use SecureString for sensitive values like database passwords.",
        "Standard vs Advanced: Standard parameters are free, support up to 10,000 parameters, 4KB size, no parameter policies. Advanced parameters cost $0.05/month, support 100,000+ parameters, 8KB size, parameter policies (expiration, notifications), and higher throughput.",
      ],
      services: ["Systems Manager Parameter Store", "KMS", "CloudTrail", "Lambda"],
      keyPoints: [
        "Hierarchical storage: /app/environment/parameter",
        "SecureString parameters encrypted with KMS",
        "Standard parameters: free, 10K limit, 4KB size",
        "Advanced parameters: $0.05/month, 100K+ limit, 8KB size, policies",
        "Integrates with CloudFormation, Lambda, EC2, ECS",
        "Versioning tracks parameter changes",
        "CloudTrail logs all parameter access",
      ],
      examTips: [
        "Parameter Store for configuration and non-rotating secrets",
        "Secrets Manager for rotating secrets",
        "SecureString uses KMS for encryption",
        "Hierarchical paths enable organized parameter management",
        "Standard parameters are free—use for cost optimization",
      ],
    },
    {
      id: "certificate-manager",
      title: "AWS Certificate Manager (ACM)",
      content: [
        "Concept: ACM provisions, manages, and deploys SSL/TLS certificates for use with AWS services. It provides free public certificates and supports importing third-party certificates. ACM automatically renews certificates before expiration.",
        "When to use: Use ACM for SSL/TLS certificates on CloudFront, ALB, API Gateway, and other integrated services. Essential for HTTPS endpoints. Public certificates are free and automatically renewed. Use for both public-facing and internal applications.",
        "When NOT to use: Don't use ACM for certificates on EC2 instances (export not supported for public certificates), for non-AWS services, or when you need certificate portability. ACM certificates are bound to AWS services and regions.",
        "Real-world scenario: A company deploys a web application behind an ALB and needs HTTPS. Request a public certificate from ACM for their domain, validate domain ownership via DNS or email, attach certificate to ALB. ACM automatically renews the certificate before expiration—no manual intervention needed.",
        "Public vs Private: Public certificates are for internet-facing applications, validated by public CAs, free. Private certificates are for internal applications, issued by ACM Private CA, charged per certificate. Use public for external sites, private for internal microservices.",
      ],
      services: ["ACM", "CloudFront", "ALB", "API Gateway", "Route 53"],
      keyPoints: [
        "Free public SSL/TLS certificates for AWS services",
        "Automatic certificate renewal before expiration",
        "Integrates with CloudFront, ALB, API Gateway, Elastic Beanstalk",
        "Domain validation via DNS (recommended) or email",
        "Regional service—certificates must be in same region as resource (except CloudFront uses us-east-1)",
        "Cannot export public certificates (can export imported certificates)",
        "ACM Private CA for internal certificates (additional cost)",
      ],
      examTips: [
        "ACM for SSL/TLS certificates on AWS services",
        "Public certificates are free and auto-renewed",
        "CloudFront requires certificates in us-east-1 region",
        "Cannot export ACM public certificates to EC2",
        "DNS validation is faster and can be automated",
      ],
    },
  ],
};
