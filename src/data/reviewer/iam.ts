import type { ReviewerContent } from "@/types";
import { DOMAIN_IDS } from "@/types";

export const iamContent: ReviewerContent = {
  domain: DOMAIN_IDS.IAM,
  sections: [
    {
      id: "overview",
      title: "IAM Overview",
      content: [
        "Identity and Access Management (IAM) is the foundation of AWS security, controlling who can access AWS resources and what actions they can perform. This domain has the highest weight (20%) on the SCS-C03 exam.",
        "IAM operates on the principle of least privilege: grant only the minimum permissions necessary to perform a task. Every permission should be justified and regularly reviewed.",
        "The key to mastering IAM is understanding policy evaluation logic: by default, everything is denied. An explicit deny always wins. Permissions are the intersection of all applicable policies.",
      ],
      keyPoints: [
        "Principle of least privilege is fundamental",
        "Use roles with temporary credentials, not long-term access keys",
        "Enable MFA for all privileged users and root account",
        "Regularly review and audit permissions using Access Analyzer",
        "Explicit deny always overrides any allow",
      ],
    },
    {
      id: "policies",
      title: "IAM Policies & Policy Evaluation",
      content: [
        "Concept: IAM policies are JSON documents that define permissions using Effect (Allow/Deny), Action (what can be done), Resource (what it applies to), and Condition (when it applies). Policies can be identity-based (attached to users/roles) or resource-based (attached to resources).",
        "When to use: Use identity-based policies for user/role permissions. Use resource-based policies for cross-account access or when the resource needs to control who can access it (S3 buckets, KMS keys, Lambda functions).",
        "When NOT to use: Don't use policies alone for organizational controls (use SCPs). Don't use overly complex policies—break them into manageable pieces. Don't grant * permissions unless absolutely necessary.",
        "Real-world scenario: A developer needs S3 access but only to objects with a specific prefix. Create an identity-based policy with Condition: s3:prefix matching 'dev/*'. This grants access to dev/ folder only, not the entire bucket.",
        "Policy Evaluation Logic: 1) By default, deny everything. 2) Evaluate all applicable policies. 3) If explicit deny exists, deny. 4) If explicit allow exists (and no deny), allow. 5) Otherwise, deny.",
      ],
      services: ["IAM", "STS", "AWS Organizations"],
      keyPoints: [
        "Policy elements: Effect, Principal, Action, Resource, Condition",
        "Explicit deny > explicit allow > implicit deny (default)",
        "Identity-based policies: attached to users, groups, roles",
        "Resource-based policies: attached to resources (S3, KMS, Lambda)",
        "Conditions enable fine-grained control (IP, time, MFA, tags)",
        "Use policy variables for dynamic permissions (${aws:username})",
      ],
      examTips: [
        "Know policy evaluation logic cold—it's tested heavily",
        "Explicit deny always wins, no exceptions",
        "Resource-based policies can grant cross-account access",
        "Conditions are key for exam scenarios (IP restrictions, MFA)",
        "NotAction is dangerous—use with explicit deny only",
      ],
    },
    {
      id: "roles-sts",
      title: "IAM Roles & STS (Temporary Credentials)",
      content: [
        "Concept: IAM roles provide temporary security credentials via AWS Security Token Service (STS). Roles have two policies: trust policy (who can assume) and permission policy (what they can do). Temporary credentials expire automatically.",
        "When to use: Use roles for EC2 instances, Lambda functions, cross-account access, federated users, and any scenario requiring temporary credentials. Always prefer roles over long-term access keys.",
        "When NOT to use: Don't use roles for long-running processes that need permanent credentials (rare case—consider alternatives). Don't use roles without proper trust policy restrictions—always limit who can assume.",
        "Real-world scenario: Company A needs to grant Company B (AWS account) access to an S3 bucket. Create a role in Account A with a trust policy allowing Account B to assume it, and a permission policy granting S3 access. Company B users assume the role to access the bucket without sharing credentials.",
        "External ID: Prevents the 'confused deputy' problem in cross-account access. Third-party requires you to provide a unique External ID in the trust policy, ensuring only they can assume your role, not another customer.",
      ],
      services: ["IAM", "STS", "AWS Organizations"],
      keyPoints: [
        "Trust policy: defines who can assume the role (Principal)",
        "Permission policy: defines what the role can do (Actions/Resources)",
        "STS provides temporary credentials (access key, secret key, session token)",
        "Credentials expire (default 1 hour, max 12 hours for roles, 36 hours for federated)",
        "External ID prevents confused deputy attacks in third-party scenarios",
        "Use sts:AssumeRole for cross-account, sts:AssumeRoleWithSAML for federation",
      ],
      examTips: [
        "Trust policy = who can assume, Permission policy = what they can do",
        "External ID is for third-party cross-account access",
        "Temporary credentials always expire—no long-term keys",
        "Session policies can further restrict assumed role permissions",
        "Know when to use AssumeRole vs AssumeRoleWithSAML vs AssumeRoleWithWebIdentity",
      ],
    },
    {
      id: "permission-boundaries",
      title: "Permission Boundaries",
      content: [
        "Concept: Permission boundaries set the maximum permissions an IAM entity (user or role) can have. They don't grant permissions—they limit them. Effective permissions are the intersection of identity policy AND permission boundary.",
        "When to use: Use permission boundaries when delegating user/role creation to prevent privilege escalation. Example: Allow developers to create roles but ensure they can't grant more permissions than their boundary allows.",
        "When NOT to use: Don't use permission boundaries as a replacement for proper IAM policies. Don't apply to groups (not supported). Don't use for organizational-level controls (use SCPs instead).",
        "Real-world scenario: A company wants to let team leads create IAM users for their teams but prevent them from creating admin users. Set a permission boundary on the team lead that limits maximum permissions to non-admin actions. Even if the team lead tries to create an admin user, the boundary prevents it.",
        "Comparison: Permission Boundary vs SCP - Permission boundary limits individual users/roles within an account. SCP limits entire accounts within an organization. Use SCPs for org-wide controls, permission boundaries for delegated administration.",
      ],
      services: ["IAM"],
      keyPoints: [
        "Permission boundary defines maximum allowed permissions",
        "Effective permissions = identity policy ∩ permission boundary",
        "Applied to users and roles only, not groups",
        "Commonly used to prevent privilege escalation during delegation",
        "Does not grant permissions, only restricts them",
        "Useful for multi-tenant environments",
      ],
      examTips: [
        "Permission boundary is a maximum limit, not a grant",
        "Intersection logic: both identity policy AND boundary must allow",
        "Cannot be applied to groups",
        "Key use case: delegated user/role creation without privilege escalation",
        "Different from SCPs—permission boundaries are account-level, SCPs are org-level",
      ],
    },
    {
      id: "comparison-iam-scp",
      title: "🔍 Service Comparison: IAM Policy vs SCP vs Permission Boundary",
      content: [
        "IAM Policy vs Service Control Policy (SCP):",
        "• IAM Policy: Controls what individual users, groups, and roles can do within an account. Grants permissions.",
        "• SCP: Controls what entire AWS accounts can do within an organization. Restricts permissions (does not grant).",
        "• Key difference: IAM policies are account-level and grant permissions. SCPs are organization-level and only restrict.",
        "• Example: IAM policy allows s3:PutObject. SCP denies s3:DeleteBucket. Result: Can upload but not delete bucket.",
        "",
        "IAM Policy vs Permission Boundary:",
        "• IAM Policy: Grants permissions to users/roles.",
        "• Permission Boundary: Sets maximum permissions a user/role can have (does not grant).",
        "• Key difference: IAM policy is what you CAN do. Permission boundary is what you CAN'T exceed.",
        "• Example: IAM policy allows s3:*, permission boundary allows s3:Get*. Result: Can only read S3, not write.",
        "",
        "SCP vs Permission Boundary:",
        "• SCP: Applies to all principals in an AWS account (organization-level).",
        "• Permission Boundary: Applies to specific users/roles (account-level).",
        "• Use SCPs for organization-wide restrictions. Use permission boundaries for delegated administration.",
        "",
        "Effective Permissions Formula:",
        "Effective Permissions = Identity Policy ∩ Permission Boundary ∩ SCP ∩ Resource Policy",
        "All must allow (except explicit deny, which always wins).",
      ],
      keyPoints: [
        "IAM Policy = grants permissions (identity or resource-based)",
        "SCP = restricts accounts in an organization (deny-only in practice)",
        "Permission Boundary = limits maximum permissions for user/role",
        "Explicit deny anywhere = denied",
        "All applicable policies must allow for access to be granted",
      ],
      examTips: [
        "If question mentions 'organization' → think SCP",
        "If question mentions 'maximum permissions' → think Permission Boundary",
        "If question mentions 'cross-account' → think Resource-based policy or Role",
        "Explicit deny always wins, regardless of source",
      ],
    },
    {
      id: "access-analyzer",
      title: "IAM Access Analyzer",
      content: [
        "Concept: IAM Access Analyzer uses automated reasoning to analyze resource policies and identify resources shared with external principals. It continuously monitors for unintended access and validates policies against best practices.",
        "When to use: Use Access Analyzer to identify resources (S3 buckets, IAM roles, KMS keys, Lambda functions) accessible from outside your account or organization. Essential for compliance audits and preventing unintended public access.",
        "When NOT to use: Access Analyzer doesn't detect overly permissive identity-based policies within your account (use Access Advisor). It doesn't prevent access—it only reports it. Not for real-time threat detection (use GuardDuty).",
        "Real-world scenario: A security team needs to ensure no S3 buckets are accidentally shared with external accounts. Access Analyzer continuously scans all resource policies and generates findings when it detects external access, including the external principal and access level.",
        "Policy Validation: Access Analyzer can validate policies before deployment, checking for syntax errors, security warnings, and suggestions. Integrates with CloudFormation and Terraform for policy-as-code validation.",
      ],
      services: ["IAM Access Analyzer", "S3", "KMS", "Lambda", "SQS", "SNS"],
      keyPoints: [
        "Analyzes resource-based policies for external access",
        "Supported resources: S3, IAM roles, KMS, Lambda, SQS, SNS, Secrets Manager",
        "Findings show external principal, resource, and access level",
        "Archive findings that are intentional (e.g., public website bucket)",
        "Policy validation checks for errors and security issues",
        "Zone of trust: your account or organization",
      ],
      examTips: [
        "Access Analyzer finds external access, not overly permissive internal access",
        "Continuously monitors resource policies, not identity policies",
        "Findings indicate access outside your zone of trust",
        "Policy validation is separate feature for checking policy syntax",
        "Archive findings that are intentional and approved",
      ],
    },
    {
      id: "mfa-security",
      title: "Multi-Factor Authentication (MFA)",
      content: [
        "Concept: MFA adds a second factor of authentication beyond username and password. AWS supports virtual MFA (app-based), hardware MFA (key fob), and U2F security keys. MFA is critical for privileged accounts and root users.",
        "When to use: Always use MFA for root account, IAM users with console access, and any privileged users. Use MFA for sensitive operations via policy conditions (aws:MultiFactorAuthPresent).",
        "When NOT to use: MFA cannot be enforced for programmatic access (API calls with access keys). For API access, use temporary credentials from roles instead of long-term keys.",
        "Real-world scenario: A company wants to ensure only MFA-authenticated users can delete S3 objects. Add a Condition to the S3 bucket policy: 'aws:MultiFactorAuthPresent': 'true'. Users without MFA cannot delete objects even if their IAM policy allows it.",
        "Best Practices: Enable MFA for root account immediately. Use virtual MFA for most users (Google Authenticator, Authy). Use hardware MFA or U2F for highest security. Enforce MFA via IAM policies with conditions.",
      ],
      services: ["IAM", "STS"],
      keyPoints: [
        "Virtual MFA: app-based (Google Authenticator, Authy)",
        "Hardware MFA: physical key fob (Gemalto)",
        "U2F security key: YubiKey (highest security)",
        "Root account MFA is critical—enable immediately",
        "Enforce MFA with policy condition: aws:MultiFactorAuthPresent",
        "MFA cannot be enforced for programmatic access (API keys)",
      ],
      examTips: [
        "Always enable MFA for root account",
        "Use aws:MultiFactorAuthPresent condition to enforce MFA for sensitive actions",
        "MFA works for console access, not API access with keys",
        "For API access security, use roles with temporary credentials",
        "U2F is most secure, virtual MFA is most common",
      ],
    },
    {
      id: "identity-federation",
      title: "Identity Federation",
      content: [
        "Concept: Identity federation allows users to access AWS using credentials from external identity providers (corporate directory, Google, Facebook) without creating IAM users. Uses SAML 2.0, OpenID Connect, or custom identity brokers.",
        "When to use: Use federation when you have existing corporate identities (Active Directory, Okta, Azure AD) and want single sign-on to AWS. Eliminates need to manage separate IAM users for each employee.",
        "When NOT to use: Don't use federation for service accounts or applications (use IAM roles). Don't use for simple scenarios with few users (IAM users may be simpler). Federation requires identity provider setup and maintenance.",
        "Real-world scenario: A company with 10,000 employees using Active Directory wants AWS access. Set up SAML federation with AD FS. Employees authenticate with AD credentials, receive a SAML assertion, and assume an IAM role via STS AssumeRoleWithSAML. No IAM users needed.",
        "Comparison: SAML 2.0 (enterprise SSO) vs OpenID Connect (web/mobile apps) vs Custom Identity Broker (legacy systems). SAML for corporate directories, OIDC for social/web identities, custom broker for proprietary systems.",
      ],
      services: ["IAM", "STS", "AWS SSO (IAM Identity Center)", "Cognito"],
      keyPoints: [
        "SAML 2.0: enterprise identity providers (AD, Okta, Azure AD)",
        "OpenID Connect: web identity providers (Google, Facebook, Amazon)",
        "Custom identity broker: for legacy or proprietary systems",
        "STS AssumeRoleWithSAML: for SAML-based federation",
        "STS AssumeRoleWithWebIdentity: for OIDC-based federation",
        "AWS SSO (IAM Identity Center): AWS-managed SSO solution",
      ],
      examTips: [
        "SAML 2.0 for enterprise federation (AD, Okta)",
        "OIDC for web/mobile app federation (Google, Facebook)",
        "Federation uses temporary credentials via STS",
        "No IAM users needed—users authenticate with external IdP",
        "Know when to use AssumeRoleWithSAML vs AssumeRoleWithWebIdentity",
      ],
    },
  ],
};
